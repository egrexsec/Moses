# Roadmap

## Maintenance-mode priorities

- keep CPU/GPU Docker paths understandable
- preserve Ableton export usefulness
- improve failure diagnostics instead of broadening scope
- document one reliable sample workflow end to end

## If active development resumes

- refresh screenshots after a verified runtime pass
- add a small smoke-test path for local validation
- simplify the UI around the main upload → separate → export workflow
